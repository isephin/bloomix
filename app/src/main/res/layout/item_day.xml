<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:gravity="center"
    android:layout_margin="2dp"
    android:minHeight="80dp">

    <!-- Flower Icon (Visible only if entry exists) -->
    <ImageView
        android:id="@+id/ivFlower"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:layout_gravity="center"
        android:scaleType="fitCenter"
        android:adjustViewBounds="true"
        android:contentDescription="flower icon"
        android:visibility="invisible" /> <!-- Hidden by default -->

    <!-- Day Number (Below Flower) -->
    <TextView
        android:id="@+id/tvDayNumber"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:text="1"
        android:textSize="20sp"
        android:textColor="#333333"
        android:fontFamily="@font/gamja_flower"
        android:textStyle="bold"
        android:gravity="center" />

</LinearLayout>