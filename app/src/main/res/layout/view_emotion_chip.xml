<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="32dp"
    android:orientation="horizontal"
    android:paddingStart="8dp"
    android:paddingEnd="12dp"
    android:paddingTop="4dp"
    android:paddingBottom="4dp"
    android:gravity="center_vertical"
    android:background="@drawable/rounded_chip_background"
    android:layout_marginEnd="8dp"
    android:layout_marginBottom="8dp">

    <!-- Use the chip background you created earlier or a default shape -->

    <ImageView
        android:id="@+id/chipImage"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:layout_marginEnd="6dp"
        tools:src="@drawable/happy_chip"
        android:contentDescription="Emotion Icon" />

    <TextView
        android:id="@+id/chipLabel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="14sp"
        android:textColor="#333333"
        tools:text="Happy" />

</LinearLayout>